# helios-bsc status

**Updated:** 2026-08-26 · **Released:** [v0.2.0](https://github.com/Alt-bi/helios-bsc/releases/tag/v0.2.0) (5 platforms, `SHA256SUMS`)

## Where the project is

A trust-minimized Parlia light client serving verified JSON-RPC on `127.0.0.1:8545`.
MVP-1 and MVP-2 are in tree. The ≥24 h differential soak passed **2026-08-24** (24.06 h,
exit 0) and 4 h followed on the shipped build (2871 compared, 0 mismatch). Block tags have
served the BEP-126 BLS-finalized head by default since **2026-08-25**.

Since **v0.2.0** nothing on a receipt is echoed from the upstream: `gasUsed` is derived
from the verified cumulative chain, and `transactionHash`, `to`, `from`, `contractAddress`
and `effectiveGasPrice` come from envelopes bound to `transactionsRoot`. `eth_getLogs`
serves ranges up to 128 blocks and poll-based filters are supported, both built from
`receiptsRoot`-bound receipts rather than an upstream's own index.

The nightly soak covers all of it — balances, every derived receipt field, every log at
the head, and a filter round trip — against a third independent host. A representative
round: `compared=3462 match=3462 mismatch=0 skip=0`.

**Not audited.** No third party has reviewed the consensus or proof code.

| | |
|---|---|
| Verified reads | balances, nonces, code, storage, `eth_call`, `eth_estimateGas`, headers, receipts, logs and log filters over ranges up to 128 blocks (≤1024 matches, else refused) |
| Read head | BLS-finalized, ~2 blocks behind the tip (falls back to ~110 without vote keys) |
| History | ~112 blocks; not an archive node |
| Tests | 471, plus an adversarial in-process lying upstream |
| Chain | BSC mainnet, `chainId` 56, pinned to `bnb-chain/bsc` v1.7.8 |
| Distribution | Five platform binaries with `SHA256SUMS`, and a multi-arch image at `ghcr.io/alt-bi/helios-bsc` (`linux/amd64`, `linux/arm64`) |

Start at [docs/quickstart.md](docs/quickstart.md); what is verified is in
[docs/rpc-matrix.md](docs/rpc-matrix.md); how each item below was found and checked is in
[docs/engineering-log.md](docs/engineering-log.md).

## Milestones

| Milestone | State |
|-----------|-------|
| Design (reviewed) | Done → `docs/design.md` |
| PR1 scaffold (workspace, licenses, CI, CLI `info`) | **Done** |
| Phase 0 hardfork pin + appendix | **Done** — `bnb-chain/bsc` **v1.7.8** `cdb7548b5baa…` |
| Phase 0 epoch fixtures | **Done** — epoch 116664000 ±2; live `n=21`, `turnLength=8` |
| extraData codec (PR 3b) | **Done** |
| ECDSA seal verify (PR 4) | **Done** — coinbase + difficulty ∈ {1,2}; in-turn match on snapshot (`--checkpoint`) |
| Epoch activation (PR 5) | **Done** — `minerHistoryCheckLen` (87 @ N=21,T=8); unit-tested |
| Confirmation-depth Safe (PR 6) | **Done** — newest Safe = 15 distinct subsequent sealers; proof window **112** |
| Phase 0 `eth_getProof` matrix | **PASS on the default build.** Measured 2026-08-21, when fast finality was still opt-in; only `--finality confirmation-depth` is a partial pass… [→ detail](docs/engineering-log.md#phase-0-eth_getproof-matrix) |
| MPT `eth_getProof` (PR 8) | **Done** — account trie vs `stateRoot`; WBNB fixture + mutated node reject; **exclusion** (absent account) → empty; **inlined** children (<32 B / nested list) for storage proofs. ≤32 nodes per account/storage proof; `getCode` ≤24576 B. |
| Local JSON-RPC (PR 9) | **Done** — wallet-mode Safe reads + verified `eth_getProof` + `helios_bsc_syncStatus` (`backupTransport`) + wallet meta + `eth_protocolVersion`… [→ detail](docs/engineering-log.md#local-json-rpc-pr-9) |
| Code / storage / broadcast (PR 11) | **Done** — `eth_getCode` (keccak vs proof `codeHash`; WBNB bytecode fixture + lying code reject), `eth_getStorageAt` (storage trie… [→ detail](docs/engineering-log.md#code-storage-broadcast-pr-11) |
| Header-verified `eth_getBlock*` | **Done** — `eth_getBlockByNumber` / `ByHash` at or below Safe; hydrated txs → `-32601`; uncle RPCs `0x0`/`null` (Parlia); `eth_coinbase` zeros. Sealed header stored at ingest — serve/persist without re-fetch; lying re-fetch ignored. |
| Verified state at `n ≤ Safe` | **Done** — `eth_getBalance` / nonce / code / storage / proof / `eth_call` / `estimateGas` at a local verified header with `n ≤ Safe` (same rule as `eth_getBlockB… [→ detail](docs/engineering-log.md#verified-state-at-n-safe) |
| Filters / subscribe | **Superseded 2026-08-25.** This row recorded the state when there was no log index: everything filter-shaped answered `-32601`. Poll-based log and block filters are now served from `receiptsRoot`-bound receipts — see the `eth_getLogs` and filter rows under *What is next*. `eth_subscribe` and pending-transaction filters stay `-32601`, and for reasons that do not expire. |
| Parent-linked header walk | **Done** — consecutive numbers + `parentHash` vs previous **computed** hash (including append stitch)… [→ detail](docs/engineering-log.md#parent-linked-header-walk) |
| Adversarial mock (PR 10) | **Done** — `helios-bsc-mock` + `RpcUpstream` trait; `Node::handle` fail-closed on lying seals/proofs/parent/`stateRoot`; 14-sealer ≠ Safe. Unproven `eth_call` SLOAD/account is fail-closed (`-32001`), not zero. |
| Constrained `eth_call` (MVP-2 slice 1) | **Done** — local verified header `n ≤ Safe` (`latest`→Safe); `to` + optional `data`; iterative `eth_getProof` at the requested hash/number; **revm 42**… [→ detail](docs/engineering-log.md#constrained-eth_call-mvp-2-slice-1) |
| Best-effort `eth_estimateGas` (MVP-2 slice 2) | **Done** — same `n ≤ Safe` constraints as `eth_call`; proof-backed revm **binary search** (geth/reth; not Helios raw `gas_used`)… [→ detail](docs/engineering-log.md#best-effort-eth_estimategas-mvp-2-slice-2) |
| Checkpoint / sealing-set (PR 13 slice) | **Done** — `--checkpoint` + persist + multisource + `verify-checkpoint`. Persist is **tmp+rename**. Sealing-set addresses must be unique 20-byte values… [→ detail](docs/engineering-log.md#checkpoint-sealing-set-pr-13-slice) |
| Soak (PR 14) | **Done (code + live ≥10 + 1h re-diff)** — unique recatch/retry; nonce vs oracle when historical nonce exists. Duration soak **re-diffs the full list** after unique is full (`visit_all`… [→ detail](docs/engineering-log.md#soak-pr-14) |
| Bind policy | **Done** — default loopback… [→ detail](docs/engineering-log.md#bind-policy) |
| Data-plane backup | **Done** — `--backup` / `HELIOS_BSC_BACKUP`: transport failover if the primary RPC errors. Both untrusted; seals/MPT still apply. Soak backup must not be the oracle host. |
| Header.Hash() | **Done** — keccak256(RLP(header)) vs RPC `hash` on every sealed header (v1.7.8 `Header.Hash()` / `gen_header_rlp.go`). Re-fetch / persist / checkpoint match the computed hash, not the JSON field. |
| Sync counters | **Done** — `syncStatus.proofOk` / `proofFail` / `headersVerified` (process lifetime). |
| Demo Slice | **Vertical closed:** Safe=15, wallet `latest`→Safe, WBNB MPT, `verify-checkpoint` GATE PASS, soak **19 unique / 214 compared / 0 mismatch** vs BlastAPI over **≥1h** re-diff. Wallet guide: `docs/quickstart.md`. |
| Operator doctor | **Done** — `helios-bsc doctor` prints RPC **hosts** only (no keys); Pasteur countdown; checkpoint age without the sealing-set list. |
| `syncStatus` lag fields | **Done** — `safeLagBlocks` / `safeLagSeconds` + `finality` (`confirmation-depth` at the time; the default became `fast-finality` on 2026-08-25). Demo Slice DoD. |
| Pasteur profile | **LIVE 2026-08-25** — activated at unix `1787625000` (02:30 UTC)… [→ detail](docs/engineering-log.md#pasteur-profile) |
| Checkpoint from epoch extraData | **Done** — `write-checkpoint --sealing-set-from-epoch` (activated extraData only; not miners). |
| Unverified passthrough (opt-in) | **Done** — `--allow-unverified-passthrough`: receipts/txs header-bound to Safe **and** to the requested 32-byte hash… [→ detail](docs/engineering-log.md#unverified-passthrough-opt-in) |
| Operator SLOs | **Done** — `docs/slo.md`; `doctor` slo=ok/warn/fail; `syncStatus.safeLagWithinBound`. |
| Prometheus metrics | **Done** — opt-in `run --metrics` → `GET /metrics` (only non-POST route… [→ detail](docs/engineering-log.md#prometheus-metrics) |
| Bootstrap epoch state | **Done** — a checkpoint carries no `turnLength` and cannot say whether a set switch is pending, so both are read back from the two epoch headers around it… [→ detail](docs/engineering-log.md#bootstrap-epoch-state) |
| **≥ 24h GA soak** | **PASS 2026-08-24** — 24.06 h continuous (2026-08-23 12:02 → 2026-08-24 12:05), exit code 0… [→ detail](docs/engineering-log.md#-24h-ga-soak) |
| 4h soak on the shipped build | **PASS 2026-08-24** — 4 h 01 m uninterrupted on the release build of this branch, with `--state`, blastapi (upstream) vs publicnode (oracle), `--finality fast`… [→ detail](docs/engineering-log.md#4h-soak-on-the-shipped-build) |
| First-run usability | **Done 2026-08-25** — the first command anyone ran demanded an epoch block number the operator had to compute as `floor(block / epochLength) * epochLength`… [→ detail](docs/engineering-log.md#first-run-usability) |
| Release binaries | **Done 2026-08-25** — `release.yml` builds on a `v*` tag for linux-x86_64 (musl, static, plus a glibc build), macOS arm64 and x86_64, and windows-x86_64… [→ detail](docs/engineering-log.md#release-binaries) |
| Bootstrap near the tip | **Fixed 2026-08-25** — `write-checkpoint --block latest` followed by `run` exited with "no Safe head in lookback"… [→ detail](docs/engineering-log.md#bootstrap-near-the-tip) |
| Superseded epoch refused | **Fixed 2026-08-25** — `--sealing-set-from-epoch` accepted any *already activated* epoch, and every epoch below the checkpoint has activated… [→ detail](docs/engineering-log.md#superseded-epoch-refused) |
| EVM fork level | **Fixed 2026-08-25** — the EVM ran at `SpecId::CANCUN` while BSC has been on **Prague since 2025-03-20** and **Osaka since 2026-04-28** (`params/config.go`: `PragueTime=1742… [→ detail](docs/engineering-log.md#evm-fork-level) |
| Checkpoint provenance | **Done 2026-08-25** — loading a checkpoint with no independent source agreeing to it now warns, naming the reason and the two flags that fix it… [→ detail](docs/engineering-log.md#checkpoint-provenance) |
| Soak finality cross-check | **Done** — the `parlia_*` comparison is the only check that tests this client's attestation bookkeeping against geth rather than against itself… [→ detail](docs/engineering-log.md#soak-finality-cross-check) |
| Soak crash resumption | **Done** — state written after every burst and again on the way out of a failed round, instead of once per round with `?` propagating past the save. Verified live: a run killed 75 s in with zero completed rounds left `compared=18 unique=12 checked_finality=1` and a 64 s session on disk. |
| Upstream connection reuse | **Done** — `ureq::post(url)` runs on a use-once `Agent`, so every JSON-RPC call paid a fresh TCP + TLS handshake. Counted through a local forwarder over one walk: **59 requests / 59 connections** before, **55 / 3** after. Reuse is worth ~275 ms per call on publicnode, ~500 ms on bsc-dataseed. |
| Refresh amplification | **Done** — every served method called `refresh()`, which always polled the upstream… [→ detail](docs/engineering-log.md#refresh-amplification) |
| Request panic containment | **Done** — a panic in `serve_one` ended the worker thread, and a panic under the chain lock poisoned it so the rest died on their next request: listener up, `/metrics` green, nobody answering. Handling now runs under `catch_unwind`, answers `-32603`, and bumps `helios_bsc_request_panics_total`. The background poller is wrapped too. |
| In-turn difficulty | **Done** — range on all headers; `inturn_validator` on checkpoint walks (padded fixture sets cannot satisfy live in-turn). |
| SignRecently (Bohr recents) | **Done** — `seenTimes >= turnLength` in `minerHistoryCheckLen`; recents cleared on set switch. Maxwell FF prune applied (see the row below). |
| Unsealed header fields | **Done** — empty uncles, gasUsed/gasLimit, `baseFeePerGas` (see below), Lorentz mixDigest ms, Bohr zero `parentBeaconRoot`, extraData ≤100KiB… [→ detail](docs/engineering-log.md#unsealed-header-fields) |
| Cascading parent fields | **Done** — `MilliTimestamp ≥ parent + BlockInterval` (Ramanujan floor); Lorentz+ gasLimit bound `parent/1024`, min 5000. Fixture 998→999 is exactly 450ms. The out-of-turn refinement of the same floor is the row below. |
| **Out-of-turn backoff** | **Done 2026-08-22** — `blockTimeVerifyForRamanujanFork` is a *verifier* rule (`verifyCascadingFields` calls it)… [→ detail](docs/engineering-log.md#out-of-turn-backoff) |
| Header-verify remaining | **Empty.** `baseFee`, out-of-turn backoff and the Maxwell prune all closed; see their rows. |
| Fixture authenticity | **Done** — `scripts/verify_fixtures.py` re-checks every fixture against live mainnet (headers field-by-field, proof `stateRoot`+`blockHash`, WBNB bytecode). Verified 2026-08-21: all pass. |
| **Fast Finality (BLS)** | **Done (consensus + observability)** — `crates/helios-bsc-consensus/src/vote.rs`: strict canonical-only RLP decode of `VoteAttestation`, `VoteData.Hash()`… [→ detail](docs/engineering-log.md#fast-finality-bls) |
| **Maxwell FF `recents` prune (BEP-524)** | **Done** — implemented from the pinned **v1.7.8** source, not from prose… [→ detail](docs/engineering-log.md#maxwell-ff-recents-prune-bep-524) |
| FF-backed block tags | **Done, now the default (2026-08-25)** — `run` resolves `latest` / `safe` / `finalized` (and the historical-read ceiling) to the BLS-finalized head, ~2 blocks instead of ~110… [→ detail](docs/engineering-log.md#ff-backed-block-tags) |
| FF verified live | **Done 2026-08-21** — ran against mainnet via `bsc-rpc.publicnode.com` from a checkpoint written with `--sealing-set-from-epoch` (21 BLS vote keys). `syncStatus`: `finality=fast-finality`, `finalizedLagBlocks=2` on four consecutive samples, `justifiedLagBlocks=1`, `safeLagBlocks` 108–113 unchanged. `/metrics`: `helios_bsc_finality_mode 1`, `finalized_lag_blocks 2`. |
| Concurrent RPC listener | **Done** — the accept loop was single-threaded, so the lock-free `/metrics` scrape still queued behind one blocked `helios_bsc_syncStatus`… [→ detail](docs/engineering-log.md#concurrent-rpc-listener) |
| Untrusted-input hardening | **Done 2026-08-22** — an audit pass over the paths that parse bytes chosen by someone else… [→ detail](docs/engineering-log.md#untrusted-input-hardening) |
| Sync/state-machine hardening | **Done 2026-08-22** — two state-corruption bugs where a *fail-closed check corrupted state on its way out*… [→ detail](docs/engineering-log.md#syncstate-machine-hardening) |
| BEP-126 checked against geth itself | **Done 2026-08-24** — the attestation path had no direct differential coverage: fixtures, plus the indirect evidence that a walk did not wedge… [→ detail](docs/engineering-log.md#bep-126-checked-against-geth-itself) |
| Soak: resumable, and wider than balances | **Done 2026-08-23** — two separate gaps, both found by actually running the gate… [→ detail](docs/engineering-log.md#soak-resumable-and-wider-than-balances) |
| BEP-126 step-1 range check | **Done 2026-08-22** — auditing the rest of `verifyVoteAttestation` after the ancestor-window bug turned up one check we never had: geth rejects `SourceNumber >= TargetNumbe… [→ detail](docs/engineering-log.md#bep-126-step-1-range-check) |
| Fermi attestation ancestor window | **Done 2026-08-22** — the client enforced the **pre-Fermi** BEP-126 rule on a Fermi chain and **wedged on honest mainnet blocks**… [→ detail](docs/engineering-log.md#fermi-attestation-ancestor-window) |
| Soak gates the head it claims | **Done 2026-08-22** — `soak --finality fast` accepted the flag and then soaked **confirmation depth**… [→ detail](docs/engineering-log.md#soak-gates-the-head-it-claims) |
| Real sealing-set tests | **Done** — `header_116663000.json` is the epoch that *governs* the fixture blocks… [→ detail](docs/engineering-log.md#real-sealing-set-tests) |
| Container image | **Done 2026-08-26** — the Dockerfile had been in the tree from the start and CI checked that it built, but the image was never published, so trying this meant cloning and compiling. `release.yml` now builds `linux/amd64` and `linux/arm64` on native runners and joins them with `docker manifest`; cross-building arm under QEMU compiles `blst` and `secp256k1` through an emulator and turns minutes into most of an hour. No third-party actions, per the rule at the top of that workflow. Verified by a live dispatch: manifest list with both architectures, anonymous pull confirmed working. |
| Open-port warning | **Done 2026-08-26** — raised by a question about the image. There is no RPC auth in this process by design (the geth/reth model: bind loopback, proxy for remote), but in a container the process *must* bind `0.0.0.0`, so the image's `CMD` carries `--allow-non-loopback` and the loopback `Host` check is inactive too. For image users both guards are pre-satisfied and only their own `-p 127.0.0.1:` prefix stands between them and an open endpoint. Nothing in-process can prevent that; what it can do is say so unmissably. The one-line warning became a marked block naming the concrete consequences, its phrases are pinned by a unit test, and `release.yml` runs the image's **default entrypoint** every release to prove the warning still appears. |
| Claims that drift | **Guarded 2026-08-26** — the test count in this file was wrong twice in one day because it is hand-written and nothing checked it. CI now compares it against what `cargo test` actually ran and fails the build on a mismatch. |
| Soak survives a restart | **Done 2026-08-26** — the round loop broke out of the run on the first `LocalRpcError`. Correct for a 45-minute CI gate; fatal for a month-long one, where the client will restart at least once and the run would report on its first days and stop. `--max-local-outage SECONDS` tolerates *transport* failures only — a mismatch still ends the run at once — and every outage is counted, timed and printed in the summary, so a run that limped cannot be read as a clean one. Default 0 keeps the CI behaviour exactly. [Runbook](docs/runbooks/long-soak.md). |
| Reorgs were never detected on the checkpoint path | **Fixed 2026-08-26** — `is_link_err` searched `err.to_string()`, and `anyhow` prints only the **outermost context**: an error built as `snap.apply_header(h).with_context(\|\| format!("snapshot {n}"))` renders as exactly `"snapshot 118189232"`, with the cause attached and unprinted. So every parent-link failure arriving through the snapshot returned `false` — and the snapshot path is what `--checkpoint` uses, i.e. the documented way to run this. The recovery branch below it had therefore never executed, which is also why nothing tested it. Not a wrong answer: a stuck one. A reorg at the chain tip left the chain on an orphaned block and every later sync failed identically, process up, answering `-32003`. `is_link_err` now walks `err.chain()`, and a test builds the **real** errors from both producing crates rather than trusting the phrase list; reverting the one line fails it. |
| Checkpoint durability | **Fixed 2026-08-26** — `atomic_write` synced the temp file and renamed it, which makes the *contents* durable and says nothing about the name now pointing at them. On Linux a rename is a directory metadata change: until the directory is fsynced, a power loss can leave no entry at all — the "operator has no trust anchor" outcome the whole temp-then-rename dance exists to prevent, and which the function's own comment argues against at length. Now fsyncs the parent directory, best-effort and silent, since Windows cannot open a directory as a file and the write has already succeeded either way. |
| Packaging audit | **Done 2026-08-26** — a sweep over what the project *ships and claims* rather than what it computes. `rust-version` said **1.80** in `Cargo.toml` and CONTRIBUTING.md while `alloy-eips` (via revm) declares **1.94.1**; cargo refuses a dependency whose floor is above the active rustc, so the tree had not built on 1.80 since revm landed and nothing said so. `tokio` was a dependency for `#[tokio::main]` over a body with no `.await` — an async runtime started and never used. Seven more dependency declarations were unused. `clippy -D warnings` was red on stable 1.96 while green on the 1.98 CI happens to run. All corrected; a `msrv` job now builds on exactly the claimed version, which goes red the moment a dependency raises its floor. |
| Supply chain in CI | **Done 2026-08-26** — `dtolnay/rust-toolchain@stable` is a *branch* and `Swatinem/rust-cache@v2` a tag: both are names their owners can move, and both run in the job that builds the release binaries, with the tree in hand before `cargo build` sees it. Pinned to commit SHAs, which dependabot updates as a readable diff. Added `advisories.yml`: RUSTSEC against `Cargo.lock` weekly and on any dependency change. Nothing checked the lockfile against the advisory database before. |
| Tables that do not render | **Guarded 2026-08-26** — two stray blank lines split the milestone table below, so its four newest rows — the container image and both guards added that day — rendered as literal pipe text on the page every reader lands on first. No test, link checker or one-row diff sees that. `scripts/check_markdown_tables.py` does, and found a second instance in `docs/README.md` the moment it was written. |
| Up vs working | **Done 2026-08-26** — a container can be up, answering on its port, and stalled: `restart: unless-stopped` only sees a process that exited, `/metrics` keeps reporting the last head that worked, and the port accepts connections regardless. `helios-bsc health` asks `helios_bsc_syncStatus`, the one call that refreshes rather than answering out of stale state, and the image now carries a `HEALTHCHECK` that runs it — no curl added, the binary was already there. The threshold is **five minutes of chain**, deliberately not the 120-block SLO: [slo.md](docs/slo.md) says a longer out-of-turn stretch is valid, and a probe that is red during normal operation is a probe people switch off. A stalled head grows by 8000 blocks an hour, so the wide bound costs nothing. Verified live against a running client: healthy at `lag=48s`, unhealthy at `--max-lag-seconds 10`, unhealthy with the server stopped. It reports and never restarts — restarting a client whose checkpoint went stale is the crash loop the runbook warns about. |
| Container gives up what it does not need | **Done 2026-08-26** — the threat model calls the container the one path where both in-process guards are inactive by construction, and until now nothing was said about the runtime the operator gets handed. `compose.yaml` now runs with `cap_drop: [ALL]`, `read_only`, `no-new-privileges` and a 16 MB tmpfs. None of it is a trade-off: the process binds a high port as `nobody`, makes outbound HTTPS, and writes exactly one file — which is why a later edit could drop any of the four without anything appearing to break, so CI asserts all four out of the rendered compose config, and `release.yml` runs the published image under the same flags every release. |
| The file an auditor pays for | **Split 2026-08-26** — `rpc_server.rs` was 4,773 lines with a single **2,594-line `impl Node`** inside it: the largest thing a reviewer has to read, and the place they are least able to say what changed. Now seven modules by surface — transport, account state, `eth_call`, blocks, receipts, logs and filters, and the shape checks on untrusted JSON — with the `Node`, sync and the dispatch table left in the root. Largest file of code: **4,234 → 1,246**. Nothing outside the file changed: not `main.rs`, not the adversarial suite. The move is *checkable* rather than asserted — `scripts/check_pure_move.py` compares the token multiset before and after and reports **39,218 → 39,218, zero added, zero removed**, with comments gained and none lost. That checker is itself mutation-tested against a flipped comparison, a dropped guard line, a changed constant and a deleted comment. |
| The first command did not work | **Fixed 2026-08-26** — `README.md`, `docs/quickstart.md` and `docs/deploy.md` all told a first-time reader to run `--upstream https://bsc-rpc.publicnode.com`, and [docs/proof-provider-matrix.md](docs/proof-provider-matrix.md) has recorded that host as **tag-only, verdict no** since 2026-08-18: it serves `eth_getProof` for the tag `latest` and refuses it at a named block, at every lag including the tip. This client can never use the tag — proving against `latest` means asking the upstream which block that is — so every verified read failed. Proven by running the same differential soak twice, changing only `--upstream`: `compared=0` with a `proof_verification_failed` on the first `eth_getBalance` against publicnode, `compared=1466 match=1466 mismatch=0` against blastapi. The measurement existed; the getting-started command ignored it. `run` now also probes its upstream at startup and prints the diagnosis, so the next tag-only provider does not need anyone to read the matrix first. |
| Reorg recovery could not reach the tip | **Fixed 2026-08-26** — with `is_link_err` repaired the recovery branch below it ran for the first time, and it had exactly one starting point: `self.origin`, the checkpoint the process booted from, fixed at bootstrap and never moved. `walk_from_checkpoint` refuses a lag above `max_sync`, so after about two hours of uptime the replay could no longer reach the tip — and no later attempt could either, because the thing it starts from only ever gets further away. A node built from a running snapshot has no origin at all and could not recover from any reorg. The client now keeps its own rollback points as it walks: one `Snapshot` clone every 16 blocks, 16 retained, **240 blocks of reach** for tens of kilobytes. Recovery truncates the chain to a point, restores the snapshot and re-walks; a point that was itself on the losing branch fails that walk at its first header and the next one back is tried, so a candidate is checked rather than assumed. Nothing is committed until a walk succeeds. Deeper than everything retained still falls back to the origin checkpoint, and says what it tried. |
| A reorg the tests could actually run | **Done 2026-08-26** — the recovery branch had no test, and could not have had one: reaching it needs two *sealed* branches of the same chain, and the mock could only mutate the five captured mainnet headers — forging an alternative branch of mainnet means forging validator signatures. `helios-bsc-mock::sealed` builds synthetic chains signed by twenty-one deterministic stand-in keys, so both branches pass the same `verify_seal_coinbase`, parent link, sealing-set, `SignRecently`, Ramanujan-floor and gas-limit checks as a captured header — valid rather than excused. Only `enforce_inturn` is off, which the live path already turns off for padded sets. Five reorg tests now drive a `Node` through real forks; reverting the rollback store fails three of them. It also opens the whole `--checkpoint` path to tests that previously had five blocks of fixtures to work with. |
| A filter that hit the log cap stayed stuck | **Fixed 2026-08-26** — a poll matching more than 1024 logs was refused with “narrow the block range”, and an `eth_getFilterChanges` caller does not choose its block range: the cursor does. Worse, the refusal left the cursor where it was, so every later poll re-read the same span and failed identically — the filter was wedged until someone re-installed it. A poll now narrows its own span and returns what fits, which is what the cursor was built for and what this method's own contract already promised. Only a single block over the cap is refused, and that message names the address/topic filter instead. `eth_getLogs` keeps the old advice, because there the caller really did pick the range. The cap is now a typed variant rather than a substring of a message: `is_link_err` made a control decision out of a string nothing pinned, and this one the compiler checks. |
| `transactionHash` was checked, never derived | **Fixed 2026-08-26** — the envelope is bound to the sealed `transactionsRoot`, so its keccak *is* the transaction hash. The client computed it, compared it to the upstream's label, and then served the label. When an upstream omitted `transactionHash` there was nothing to compare against and the field was left null — on a receipt this client had everything it needed to label, and which `eth_getTransactionReceipt` could then not match to the hash it was asked for. Now derived and stamped on the receipt and on every log it carries, so `docs/rpc-matrix.md`'s “nothing on a receipt is echoed” holds for this field too. |
| The size of the audit was a number nobody could check | **Fixed 2026-08-26** — the figure quoted for how much code an audit would have to cover was **~11,300 lines, tests excluded**, and it was wrong twice over. Two of its four rows were plain `wc -l`: `rpc_server.rs` (4,773) and `sync.rs`+`upstream.rs` (1,686) are exactly the raw sizes those files had at v0.2.0, so **1,138 lines of test code** sat inside a total headed *tests excluded* — on its own stated rule the figure was ~10,200. In the other direction it left out code that plainly belongs: `main.rs`, which loads, confirms and writes the **checkpoint** every later proof is checked against, plus the fork schedule in `helios-bsc-config` that decides which consensus rules run at all. A number that is quoted where the code is not is a number that quietly becomes false, so it is now generated rather than estimated: `scripts/audit_scope.py` strips `#[cfg(test)]` items by brace depth, lists the eight areas in scope and names the five it excludes and why, and prints **13,609**. It was checked against a hand count of `sync.rs`+`upstream.rs` (1,196, from the two `#[cfg(test)]` line numbers) before being believed. CI runs it with a **5% band** — wide enough that ordinary growth does not fire it, because a guard that cries wolf gets muted and a muted guard is worse than none. |
| The release pipeline was rejected, and looked like noise | **Fixed 2026-08-26** — pinning the actions to commit SHAs on 2026-08-26 left one step in `release.yml` with **two `with:` blocks**, and GitHub rejects the whole file for that. Every push since 09:41 that day logged a 0-second failed run with **no jobs in it**, and the next `v*` tag would have produced no binaries, no image and no release — the failure was in the file, so nothing inside it ever ran. It read as noise for a precise reason: that workflow only triggers on tags, so a red mark against a branch push looked like something that could not matter. It was found by asking why a run existed at all when no trigger matched. `scripts/check_workflows.py` now refuses a duplicate key at any depth, plus a job with no `runs-on` or `steps` and a step with neither `run` nor `uses` — each rule mutation-tested by reintroducing the defect and watching it fail. The obvious guard would not have worked: `yaml.safe_load` accepts a duplicate key and keeps the last one, so a plain parse of the broken file passes. |
| A fork watch that had never run once | **Fixed 2026-08-26** — `upstream-pin.yml` was added on 2026-08-25 with a Monday cron, so its first scheduled run was still days away and **nothing had ever executed it**. Dispatched by hand it failed immediately, and for a reason worth keeping: it selected `v1.8.0-alpha` as the newest release despite a comment saying alphas are never a pin target. The filter trusted the GitHub `prerelease` flag, which bnb-chain sets **by hand and inconsistently** — `v1.7.0-alpha` carries it, `v1.8.0-alpha` does not. Left alone the watch would have failed every Monday over a release it must never pin to, which is how a guard gets muted, and a muted guard is worse than none. The tag name is now checked as well as the flag, and a pre-release newer than every stable one is reported as a **notice** rather than a failure — losing that early sight of a fork was the other way to get this wrong. Verified against the live API: newest stable `v1.7.8` equals the pin, so the job passes, while still saying that the alpha touches `parlia.go` and `params/config.go`. What it found is under *What is next*, item 6. |
| The release path could not be rehearsed | **Fixed 2026-09-02** — `release.yml` runs on a `v*` tag and nothing else, so nothing exercised it until the tag that needed it. `check_workflows.py` now refuses a file GitHub would reject, but a file that parses can still be a pipeline that does not work, and an action that changed its inputs under a major bump is the ordinary way that happens — two such bumps were open (`actions/upload-artifact` v4→v7, `actions/download-artifact` v4→v8, four majors between them) and CI could say nothing about either. `workflow_dispatch` now takes `dry_run`, **default true**: a manual run builds all five targets, uploads, downloads and checksums exactly as a tag would, and publishes nothing. The per-arch `docker push` is gated and the `manifest` job is skipped outright — `docker manifest create` reads the images back out of the registry, so with nothing pushed it could only fail, and a rehearsal that always ends red is one nobody reads. Both halves of the condition are load-bearing: on a tag there is no `inputs` context at all, so `!inputs.dry_run` alone would be right only by accident of null-coercion, and `github.event_name != 'workflow_dispatch'` is what keeps a real release independent of the input entirely. Proven by run [33594558224](https://github.com/Alt-bi/helios-bsc/actions/runs/33594558224): five builds green, `Push` **skipped**, `manifest` **skipped**, and the `release` job downloading `Total of 5 artifact(s)` under v8's `digest-mismatch: error` and writing one flat `SHA256SUMS` — which is the `merge-multiple` contract the bump could have broken, checked rather than argued. |
| The fork schedule was transcribed and never checked | **Fixed 2026-09-02** — eleven constants in `helios-bsc-config`, `LONDON_BLOCK` through `PASTEUR_TIME`, were copied by hand out of geth's `params/config.go`. They decide which consensus rules apply at a given header, so a mistyped digit does not crash: it verifies a block under the wrong rules and answers confidently, which is the worst failure this codebase can have. Nothing checked the copy. `cargo test` cannot — the tests assert the constants against themselves — and `upstream-pin.yml` cannot either, because it only diffs upstream against upstream, so a number wrong on day one stays wrong and every guard in the tree agrees with it. `scripts/check_fork_schedule.py` reads the constants **and the pin** out of the same Rust file, fetches `params/config.go` at that commit and compares field by field; it also checks that `OsakaTime` and `MendelTime` still coincide, since one constant standing for two fields is itself a claim, and it refuses to pass unless it compared every constant it names. Against v1.7.8 **all eleven match** — the transcription was right, and is now checked. Mutation-tested four ways (wrong digit, deleted constant, fork moved upstream, Osaka/Mendel pulled apart). It runs in `upstream-pin.yml` where any failure is fatal, and in `ci.yml` where the script's distinct exit 3 for *upstream unreachable* is a notice — a guard that goes red for someone else's outage gets muted. |
| A row in the RPC matrix rendered one column out of place | **Fixed 2026-09-02** — `docs/rpc-matrix.md` is the document that says what is verified, what is passed through and what is refused. Its `Block-tag hex` row had two cells under a three-column header, and GFM pads such a row at the **end**, shifting every cell left: the whole note about canonical `0x` parsing rendered inside the *Trust* column and the row's trust class was blank. Every word was present and correct and the table still said something else. `check_markdown_tables.py` existed for this class and could not see it — it checked that a `|` line is inside a table, not that its cells line up — so it now checks both, counting cells with a scan rather than a `split` because a `|` inside a code span or written `\|` does not separate cells and this file is full of the former. |

## What is next

The MVP-1 and MVP-2 gates are closed and v0.2.0 has shipped. What is open:

1. **No audit.** Nothing here has been reviewed by a third party. This is the single
   largest gap between the current state and anything anyone should hold funds behind.
   The surface that would need reviewing is **~13,600 lines**, tests excluded — the code
   where a defect yields a *wrong verified answer* rather than a crash or a refusal. That
   figure is generated by `python scripts/audit_scope.py`, which prints the per-area
   breakdown and names what it leaves out, so nobody has to take it on trust; CI fails if
   the code drifts more than 5% away from it.
2. **The 30-day soak has not been run.** It is the other thing standing between this
   and anything anyone should hold funds behind, and unlike the audit it needs nothing
   but a host and a month. The nightly CI soak is **not** it and cannot be: each run is
   45 minutes, starts from a fresh `soak-state.json`, and is scheduled best-effort —
   GitHub dropped the run of 2026-08-27 entirely and fired 08-28 eighteen hours late. So
   thirty green nights are thirty independent short runs, not one long one, and the
   failures a month-long run exists to find — state that drifts, a client that restarts,
   an outage that has to be tolerated without being hidden — are exactly the ones a
   45-minute run cannot see. `--max-local-outage` and the resumable state landed for this
   run and have never been exercised at length. The procedure, the flags and what counts
   as a pass are in [docs/runbooks/long-soak.md](docs/runbooks/long-soak.md);
   `--rounds 43200 --interval 60` is roughly the month.

3. **`eth_subscribe` and pending-transaction filters.** Pub/sub needs a WebSocket
   transport this server does not have, and a pending-transaction filter would have to
   report unmined state, which no sealed header can prove. Both stay `-32601`. Log ranges
   and poll-based filters (`eth_newFilter` / `eth_newBlockFilter` /
   `eth_getFilterChanges` / `eth_getFilterLogs` / `eth_uninstallFilter`) closed on
   2026-08-25 — up to 128 blocks, built from `receiptsRoot`-bound receipts rather than
   from an upstream's own `eth_getLogs`. A filter whose range has grown past the cap gets
   an error from `eth_getFilterLogs` naming the span; `eth_getFilterChanges` keeps
   draining it 128 blocks at a time.
4. **Receipt fields — closed 2026-08-25.** Nothing on a receipt is echoed from the
   upstream any more. `gasUsed` is derived from the verified cumulative chain;
   `transactionHash` is bound to `transactionsRoot`; and `to`, `from`, `contractAddress`
   and `effectiveGasPrice` come from the bound envelope, `from` by recovering the
   signature over it. This needs the upstream to serve raw envelopes — where it does
   not, the values are unprovable rather than wrong and the echoed ones stand.
5. **The soak needs an oracle serving `parlia_`.** With `--finality fast` the run
   cross-checks its justified/finalized pair against geth's own answer and fails closed if
   that check never produced a verdict. Most public BSC endpoints answer `-32601`;
   `bsc-rpc.publicnode.com` serves it, which is why it is the oracle in CI.
6. **Re-pin when a fork changes a Parlia rule.** `upstream-pin.yml` watches `bnb-chain/bsc`
   weekly and diffs the four files this client transcribes. Pasteur (2026-08-25) changed
   none of them; the pin is still v1.7.8, which is still the newest stable release.
   **`v1.8.0-alpha` (2026-08-13) does touch two of them**, and the watch now reports a
   pre-release as a notice rather than re-pinning to it. `params/config.go` is a rename
   (`VerkleTime` → `UBTTime`) that this client does not read. `consensus/parlia/parlia.go`
   is 25 changed lines, all inside system-transaction application — `uint256` money types
   and a new gas-budget API — and none of it is in header verification, seal recovery,
   snapshot transitions or backoff, which is what this client actually transcribes. One
   line in it is worth watching rather than ignoring: the system receipt now continues a
   *refund-inclusive* cumulative chain while `usedGas` carries a refund-exclusive one, per
   **EIP-7778**. This client derives receipt `gasUsed` from cumulative deltas, so if that
   rule ships, the derivation has to be re-read against it. Not before: the fork is not
   live, and implementing an unshipped rule from a diff is how a client breaks on the day
   the rule arrives.

## What the mainnet scan showed (2026-08-21) and how each item closed

Sampled **2400 headers** across ~1.8M blocks of history (115.4M → 117.24M, `bsc-dataseed.bnbchain.org`), plus **600 consecutive** at the tip:

| Observed | Count |
|----------|-------|
| `difficulty` = `0x2` (in-turn) | **2400 / 2400** — no out-of-turn block found |
| `baseFeePerGas` = `0x0` | **2400 / 2400** |
| `gasLimit` = `0x3473bc0` | **2400 / 2400** (never moved) |
| MilliTimestamp gap | **exactly 450 ms**, 599 / 599 consecutive pairs |

What this means for each item:

- **Out-of-turn backoff** — **closed 2026-08-22.** The scan is why it stayed open: it only constrains `difficulty == 1` headers and found none, so no fixture can exercise it against real data, and a check invented from prose would fire first during a validator outage — i.e. break the client exactly when out-of-turn blocks finally appear. What changed is that it was *not* invented from prose: the pinned v1.7.8 `ramanujanfork.go` / `parlia.go` are readable, and the only unreproducible input (a Go `math/rand` shuffle) turns out to be a **non-negative additive term** that a sound floor can simply drop. Every remaining unknown is an explicit no-op branch, so the outage failure mode above is structurally impossible: with no trustworthy sealing set or an incompletely walked `recents` window the rule does not run at all. Synthetic-walk tests cover both sides of every gate; the five live fixtures are all in-turn and are asserted **unaffected**.
- **EIP-1559 `baseFee`** — **closed 2026-08-22**, and the scan was right that "the Ethereum formula is not the rule here". The source says so outright: `CalcBaseFee` opens with `if config.IsInBSC() { return InitialBaseFeeForBSC }`, `IsInBSC()` is `c.Parlia != nil`, and `InitialBaseFeeForBSC` is `0`. There is no parent-dependent formula on BSC to port — `VerifyEIP1559Header` reduces to *present after London, absent before, and equal to zero*, which is what `seal.rs::verify_base_fee` enforces. **This is not a new defence against a lying RPC** and is not claimed as one: `baseFeePerGas` is inside `EncodeSigHeader`, so a restated value already breaks the seal. It buys a precise error instead of a signature mismatch, and it makes our verifier surface match geth's exactly.
- **Maxwell FF recents prune** — **closed** earlier, same way: nine lines of readable Go in the pinned `v1.7.8` `consensus/parlia/snapshot.go`, implemented from source and cited. Unit-tested in `snapshot.rs` on both sides of the fork gate.

General point, unchanged: all three are *protocol well-formedness* rules the sealing set already enforces, and all three fields are seal-protected (`live_epoch_set_rejects_restated_difficulty_via_seal`). Against a lying **RPC** they add nothing. They bind a malicious **validator**, which already requires 15-of-21 collusion — outside the stated trust model. They were low-priority hardening, and they are now done; the GA blocker was never these.
### Other items the scan closed

- `eth_estimateGas` best-effort — **closed** (proof-backed binary search; MethodPolicy Verified; never proxied). Local `BLOCKHASH` — **closed**. Revert/Halt JSON-RPC **code 3** — **closed** (geth; not `-32001`).
- Fast Finality BLS — **implemented and verified** (see the row above). The Maxwell FF `recents` prune that it unblocked is now **done** (its own row above). That follow-up — serving the block tags from BLS finality — **shipped 2026-08-25** as the default, gated on the ≥24 h soak that passed the day before. Note the prune's companion fix made `finalized()` **lag correctly**: it now only advances on two consecutive justified blocks, per `updateAttestation`, so anything reading it moves slightly later than before. Because BLS finality lands **2 blocks** behind the tip instead of 106–112, the `eth_getProof` provider window stops being the binding constraint it is in [docs/proof-provider-matrix.md](docs/proof-provider-matrix.md) — but only once the tag actually resolves there.
- `receiptsRoot` RPC proofs on mined receipts/logs; `eth_getLogs` — **closed** (`bin/helios-bsc/src/rpc_server.rs`: untrusted receipt JSON re-encoded to consensus RLP and bound to the sealed `receiptsRoot` via `verify_receipt_list`; `eth_getLogs` is single-block only — `fromBlock==toBlock` or `blockHash` — adversarial-tested against omitted/lying logs). `transactionsRoot` hashes in `eth_getBlock*` — **closed** (raw-RLP DeriveSha; empty root or no envelopes → `[]`; lying list → `-32001`). Count/ByIndex **Verified**. Opt-in `eth_getRawTransactionByHash` — **closed** (keccak-bound). `accessList` prefetch — **closed**. Historical `n≤Safe` — **closed**. **Superseded 2026-08-25.** This entry recommended pinning `SpecId::CANCUN`; that recommendation was wrong, because BSC had been on Prague since 2025-03-20 and Osaka since 2026-04-28, so the client was executing under rules the chain had left. The spec is now derived from the executing block's timestamp — see *EVM fork level* in [docs/engineering-log.md](docs/engineering-log.md), including the EIP-7702 hypothesis that the live test disproved.

## Live pins (do not assume design-doc 16)

| Param | Value |
|-------|------:|
| epochLength | 1000 |
| turnLength | **8** |
| N_seal | 21 |
| minerHistoryCheckLen | 87 |
| proof window | 112 |
| Safe lag | ~106–112 live / 120 in-turn upper |
